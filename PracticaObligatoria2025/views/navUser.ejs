<div class="row border border-2 border-secondary mb-4">
    <nav class="navbar navbar-expand-lg navbar-light bg-light w-100">
        <div class="container-fluid p-0">
            <button class="navbar-toggler mx-auto mb-1" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <!-- Menús fuera del menú hamburguesa -->
                <ul class="navbar-nav justify-content-center w-100 d-none d-lg-flex">
                    <li class="nav-item mb-1 mx-3">
                        <form action="/usuarios/" method="GET" class="d-flex" role="search"> 
                            <button type="submit" class="btn">Eventos</button>
                        </form>   
                    </li>
                    <li class="nav-item mb-1 mx-3">
                        <form action="/usuarios/misInscripciones" method="GET" class="d-flex" role="search"> 
                            <button type="submit" class="btn">Mis Inscripciones</button>
                        </form>   
                    </li>
                    <li class="nav-item mb-1 mx-3">
                        <form action="/usuarios/miperfil" method="GET" class="d-flex" role="search"> 
                            <button type="submit" class="btn">Mi Perfil</button>
                        </form>   
                    </li>
                    <li class="nav-item mb-1 mx-3">
                        <form action="/usuarios/misnotificaciones" method="GET" class="d-flex" role="search"> 
                            <button type="submit" class="btn">Mis Notificaciones</button>
                        </form>   
                    </li>
                    <% if (rol === 'organizador') { %>
                        <li class="nav-item mb-1 mx-3">
                            <button type="button" class="btn" id="btnCrearEvento">Crear Evento</button>
                        </li>
                    <% } %>
                    <% if (rol === 'organizador') { %>
                        <li class="nav-item mb-1 mx-3"></li>
                            <form action="/eventos/misEventos" method="GET" class="d-flex" role="search">
                                <button type="submit" class="btn">Mis Eventos</button>
                            </form>
                        </li>
                    <% } %>
                    <li class="nav-item mb-1 ms-auto mx-5">
                        <form action="/logout" method="GET" class="d-flex" role="search"> 
                            <button type="submit" class="btn" id="btnClose">Cerrar Sesión</button>
                        </form>   
                    </li>
                </ul>

                <!-- Menús dentro del menú hamburguesa -->
                <ul class="navbar-nav me-auto mb-2 mb-lg-0 d-lg-none text-start">
                    <li class="nav-item mb-2">
                        <form action="/usuarios/" method="GET" class="d-flex w-100" role="search">
                            <button type="submit" class="btn w-100">Eventos</button>
                        </form>
                    </li>
                    <li class="nav-item mb-2">
                        <form action="/usuarios/misInscripciones" method="GET" class="d-flex w-100" role="search">
                            <button type="submit" class="btn w-100">Mis Inscripciones</button>
                        </form>
                    </li>
                    <li class="nav-item mb-2">
                        <form action="/usuarios/miperfil" method="GET" class="d-flex w-100" role="search">
                            <button type="submit" class="btn w-100">Mi Perfil</button>
                        </form>
                    </li>
                    <li class="nav-item mb-2">
                        <form action="/usuarios/misnotificaciones" method="GET" class="d-flex w-100" role="search">
                            <button type="submit" class="btn w-100">Mis Notificaciones</button>
                        </form>
                    </li>
                    <% if (rol === 'organizador') { %>
                        <li class="nav-item mb-2">
                            <button type="button" class="btn w-100" id="btnCrearEvento">Crear Evento</button>
                        </li>
                    <% } %>
                    <% if (rol === 'organizador') { %>
                        <li class="nav-item mb-2">
                            <form action="/eventos/misEventos" method="GET" class="d-flex w-100" role="search">
                                <button type="submit" class="btn w-100">Mis Eventos</button>
                            </form>
                        </li>
                    <% } %>                    
                    <li class="nav-item mt-4">
                        <form action="/logout" method="GET" class="d-flex w-100" role="search">
                            <button type="submit" class="btn w-100" id="btnClose">Cerrar Sesión</button>
                        </form>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</div>
